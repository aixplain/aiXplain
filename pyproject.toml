# aixplain/pyproject.toml

[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["aixplain"]
namespaces = true

[project]
name = "aiXplain"
version = "1.0.3"
description = "Mock description"
license = { text = "Mock license" }
