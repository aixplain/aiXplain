name: Run Tests

on:
  push:
    branches:
      - main
      # any branch other than main, will use the test key
      - test
  workflow_dispatch:

env:
  # Coverage floor for the unit suite, enforced by the `unit-coverage` job.
  # Measured at 64% on `development` @ 42ed1e02; set two points below to absorb
  # ordering/version jitter. Ratchet it up here as coverage improves.
  COVERAGE_FLOOR: "62"
  # A leg that collects tests, skips every one, and exits 0 is indistinguishable
  # from a leg that passed -- `agent` and `team_agent` reported green while
  # executing 0 of 50 collected tests (ENG-3544). With this set, the
  # `pytest_sessionfinish` guard in tests/conftest.py fails any session in which
  # no test body ran.
  AIXPLAIN_REQUIRE_EXECUTED_TESTS: "1"

jobs:
  # The unit suite is deliberately credential-free: no TEAM_API_KEY, no
  # BACKEND_URL. If this job starts failing on a missing key, the regression is
  # in the code under test (something validating credentials at import time),
  # not in this workflow.
  unit-coverage:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # `coverage[toml]` (not plain `coverage`): the coverage settings live in
          # pyproject.toml, and on Python < 3.11 coverage needs the `tomli` extra to
          # read it. Without the extra it aborts with "Can't read 'pyproject.toml'
          # without TOML support" before a single test runs.
          pip install ".[test]" "coverage[toml]"

      - name: Run unit tests with coverage
        run: coverage run -m pytest tests/unit

      - name: Enforce coverage floor
        run: coverage report --fail-under=${{ env.COVERAGE_FLOOR }}

      - name: Write coverage.xml
        if: always()
        run: coverage xml

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-xml
          path: coverage.xml

  setup-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 45  # Global timeout fallback
    strategy:
      fail-fast: false
      matrix:
        # 'unit' is absent on purpose: it runs in the credential-free
        # `unit-coverage` job above, so it is not executed twice.
        # Every directory under tests/functional/ must be claimed by exactly one
        # leg here; tests/unit/test_ci_matrix_coverage.py asserts it, so an
        # orphaned test file (04dea96e removed four leg names but kept their
        # test files) is now a unit-test failure rather than silent dead code.
        test-suite: [
          'file_asset',
          'data_asset',
          'benchmark',
          'model',
          'pipeline_2.0_v1',
          'pipeline_designer',
          'pipeline_create',
          'general_assets',
          'apikey',
          'agent',
          'team_agent',
          'v2',
        ]
        include:
          - test-suite: 'file_asset'
            path: 'tests/functional/file_asset'
            timeout: 45
          - test-suite: 'data_asset'
            path: 'tests/functional/data_asset'
            timeout: 45
          # Restores functional coverage for MetricFactory/BenchmarkFactory,
          # which had none after 04dea96e dropped this leg name. The suite has a
          # 30-minute internal TIMEOUT, so 45 leaves it room to report.
          - test-suite: 'benchmark'
            path: 'tests/functional/benchmark'
            timeout: 45
          - test-suite: 'model'
            path: 'tests/functional/model'
            timeout: 45
          - test-suite: 'pipeline_2.0_v1'
            path: 'tests/functional/pipelines/run_test.py --pipeline_version 2.0 --sdk_version v1 --sdk_version_param PipelineFactory'
            timeout: 45
          # No --sdk_version/--pipeline_version flags on these two on purpose:
          # the filters in tests/conftest.py drop every item that lacks the named
          # parameter, so passing them to a non-parametrised file would collect N
          # and execute 0 -- the very thing this ticket removes.
          - test-suite: 'pipeline_designer'
            path: 'tests/functional/pipelines/designer_test.py'
            timeout: 45
          - test-suite: 'pipeline_create'
            path: 'tests/functional/pipelines/create_test.py'
            timeout: 45
          - test-suite: 'general_assets'
            path: 'tests/functional/general_assets'
            timeout: 45
          - test-suite: 'apikey'
            path: 'tests/functional/apikey'
            timeout: 45
          - test-suite: 'agent'
            path: 'tests/functional/agent'
            timeout: 45
          - test-suite: 'team_agent'
            path: 'tests/functional/team_agent'
            timeout: 45
          - test-suite: 'v2'
            path: 'tests/functional/v2'
            timeout: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ".[test]"

      - name: Set environment variables
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "TEAM_API_KEY=${{ secrets.TEAM_API_KEY_PROD }}" >> $GITHUB_ENV
            echo "BACKEND_URL=https://platform-api.aixplain.com" >> $GITHUB_ENV
            echo "MODELS_RUN_URL=https://models.aixplain.com/api/v1/execute" >> $GITHUB_ENV
            echo "PIPELINES_RUN_URL=https://platform-api.aixplain.com/assets/pipeline/execution/run" >> $GITHUB_ENV
          else
            echo "TEAM_API_KEY=${{ secrets.TEAM_API_KEY }}" >> $GITHUB_ENV
            echo "BACKEND_URL=https://test-platform-api.aixplain.com" >> $GITHUB_ENV
            echo "MODELS_RUN_URL=https://test-models.aixplain.com/api/v1/execute" >> $GITHUB_ENV
            echo "PIPELINES_RUN_URL=https://test-platform-api.aixplain.com/assets/pipeline/execution/run" >> $GITHUB_ENV
          fi
          echo "SLACK_TOKEN=${{ secrets.SLACK_TOKEN }}" >> $GITHUB_ENV
          echo "HF_TOKEN=${{ secrets.HF_TOKEN }}" >> $GITHUB_ENV

      - name: Run Tests
        timeout-minutes: ${{ matrix.timeout }}
        run: python -m pytest ${{ matrix.path }}
